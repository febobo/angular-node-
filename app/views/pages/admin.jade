extends ../layout
block content
	.container.mt20(ng-app="febobo")
		.row(ng-controller="adminController")
			.list-group.col-sm-2
				a.list-group-item.active.disabled(javascript="void(0)") 网站管理
				a.list-group-item(ng-repeat="item in vm.group" href="{{item.href}}" target="febobo") {{item.title}}
			.col-sm-10.pn
				iframe.oh(src="category" frameborder="0" width="100%;" height="500px" scrolling="no" name="febobo")
	script(src="/js/main.js")
	script(src="/js/service/service.js")
	script(src="/js/directive/directive.js")

	script(src="/js/controller/admin.js")